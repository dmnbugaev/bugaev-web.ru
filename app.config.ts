export default defineAppConfig({
  // TOGGLE REVIEWS: измени showReviews на true, чтобы снова показать блок отзывов на главной странице.
  // Данные хранятся в components/Testimonials.vue
  showReviews: false,
})
